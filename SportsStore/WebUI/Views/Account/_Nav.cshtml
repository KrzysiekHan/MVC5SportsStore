<ul class="nav navbar-nav navbar-right">
    @if (@User.Identity.IsAuthenticated)
    {       
        <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <span class="glyphicon glyphicon-user" data-toggle="tooltip" title="@User.Identity.Name" data-placement="bottom"></span>
                <span class="caret"></span>
            </a>           
            <ul class="dropdown-menu">
                <li><a href="@Url.Action("Index","Admin")">Lista produktów</a></li>
                <li><a href="@Url.Action("List","Order")">Historia zakupów</a></li>
            </ul>
        </li>
        <li><a href="@Url.Action("Logout","Account",new { username = @User.Identity.Name })"><span class="glyphicon glyphicon-log-out"></span> Wyloguj </a></li>
    }
    else
    {
        <li><a href="@Url.Action("Index","Admin")"><span class="glyphicon glyphicon-user"></span> Zaloguj </a></li>
    }
</ul>

